const authController = require("./auth");
const googleAuthController = require("./auth/google");
const organizationController = require("./organization");

module.exports = {
  authController,
  googleAuthController,
  organizationController,
};
